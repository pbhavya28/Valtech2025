spring:
  application:
    name: First
  datasource:
    driver-class: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/training
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: create